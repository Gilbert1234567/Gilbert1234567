/*Poniter; C program to swap two numbers using reference */
/* Gilbert Kipkech*/
/* Roll No. 33*/
/* Date 22.10.2024*/

#include <stdio.h>

	main() {
    int x = 10, y = 20;
    int *a = &x;
    int *b = &y;
    int temp;

    printf("Before swapping: x = %d, y = %d\n", x, y);
    
    temp = *a;
    *a = *b;
    *b = temp;

    printf("After swapping: x = %d, y = %d\n", x, y);
}
